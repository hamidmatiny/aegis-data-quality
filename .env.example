# AEGIS Data/Quality Analyst Configuration
# Copy this to .env if you later add feed/API keys.
# Trinity connection is NOT configured here — the platform injects it.

# Day-one review uses Trinity MCP report tools; no required secrets.
