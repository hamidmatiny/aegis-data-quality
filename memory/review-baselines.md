# Review baselines

Append-only notes from `/review-fleet-outputs` and `/trial-review`.
Schema expectations and last-seen values go here — never invent a field that a source did not return.

_(empty — first review will append)_
